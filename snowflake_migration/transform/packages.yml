packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]

# The Impact-CI packages (facebook, ga4, google_ads, dv360, ttd, cm360, hivestack,
# tiktok, linkedin, dash_table_general_process, testing_sum) are BigQuery-dialect
# only and will not compile against Snowflake. Add them back here once a
# Snowflake-compatible revision exists.
